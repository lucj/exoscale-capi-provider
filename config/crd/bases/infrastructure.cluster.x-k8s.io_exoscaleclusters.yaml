---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
  name: exoscaleclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ExoscaleCluster
    listKind: ExoscaleClusterList
    plural: exoscaleclusters
    singular: exoscalecluster
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.ready
          name: READY
          type: string
        - jsonPath: .spec.zone
          name: ZONE
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: ExoscaleCluster is the Schema for the exoscaleclusters API.
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              description: ExoscaleClusterSpec defines the desired state of ExoscaleCluster.
              properties:
                controlPlaneEndpoint:
                  description: ControlPlaneEndpoint represents the endpoint used to communicate with the control plane.
                  properties:
                    host:
                      description: The hostname on which the API server is serving.
                      type: string
                    port:
                      description: The port on which the API server is serving.
                      format: int32
                      type: integer
                  required:
                    - host
                    - port
                  type: object
                masterSecurityGroup:
                  description: MasterSecurityGroup is the name of the security group for control-plane nodes.
                  type: string
                nodeSecurityGroup:
                  description: NodeSecurityGroup is the name of the security group for worker nodes.
                  type: string
                zone:
                  description: Zone is the Exoscale zone where cluster resources will be created.
                  type: string
              required:
                - zone
              type: object
            status:
              description: ExoscaleClusterStatus defines the observed state of ExoscaleCluster.
              properties:
                conditions:
                  description: Conditions is a list of conditions for the ExoscaleCluster.
                  items:
                    description: Condition defines an observation of a Cluster API resource operational state.
                    properties:
                      lastTransitionTime:
                        description: Last time the condition transitioned from one status to another.
                        format: date-time
                        type: string
                      message:
                        description: A human readable message indicating details about the transition.
                        type: string
                      reason:
                        description: The reason for the condition's last transition in CamelCase.
                        type: string
                      severity:
                        description: Severity provides an explicit classification of Reason code.
                        type: string
                      status:
                        description: Status of the condition, one of True, False, Unknown.
                        type: string
                      type:
                        description: Type of condition in CamelCase.
                        type: string
                    required:
                      - lastTransitionTime
                      - status
                      - type
                    type: object
                  type: array
                controlPlaneEndpoint:
                  description: ControlPlaneEndpoint is the endpoint used to communicate with the control plane.
                  properties:
                    host:
                      description: The hostname on which the API server is serving.
                      type: string
                    port:
                      description: The port on which the API server is serving.
                      format: int32
                      type: integer
                  required:
                    - host
                    - port
                  type: object
                eipAddress:
                  description: EIPAddress is the IP address of the Elastic IP.
                  type: string
                eipID:
                  description: EIPID is the ID of the Elastic IP allocated for the control plane endpoint.
                  type: string
                masterSecurityGroupID:
                  description: MasterSecurityGroupID is the ID of the security group used by control-plane nodes.
                  type: string
                nodeSecurityGroupID:
                  description: NodeSecurityGroupID is the ID of the security group used by worker nodes.
                  type: string
                ready:
                  description: Ready indicates that the cluster is ready.
                  type: boolean
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
