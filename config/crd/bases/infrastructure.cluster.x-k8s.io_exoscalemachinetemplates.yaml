---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
  name: exoscalemachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ExoscaleMachineTemplate
    listKind: ExoscaleMachineTemplateList
    plural: exoscalemachinetemplates
    singular: exoscalemachinetemplate
  scope: Namespaced
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          description: ExoscaleMachineTemplate is the Schema for the exoscalemachinetemplates API.
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              description: ExoscaleMachineTemplateSpec defines the desired state of ExoscaleMachineTemplate.
              properties:
                template:
                  description: ExoscaleMachineTemplateResource defines the template resource for ExoscaleMachine.
                  properties:
                    spec:
                      description: ExoscaleMachineTemplateResourceSpec defines the user-settable machine fields.
                      properties:
                        antiAffinityGroup:
                          description: AntiAffinityGroup is the optional name or ID of an anti-affinity group.
                          type: string
                        diskSize:
                          description: DiskSize is the size of the instance disk in GB.
                          format: int64
                          type: integer
                        instanceType:
                          description: InstanceType is the name or ID of the Exoscale instance type.
                          type: string
                        ipv6:
                          description: IPv6 enables IPv6 networking on the instance.
                          type: boolean
                        sshKey:
                          description: SSHKey is the name of the SSH key pair to use for the instance.
                          type: string
                        template:
                          description: Template is the name or ID of the Exoscale compute instance template.
                          type: string
                        zone:
                          description: Zone is the Exoscale zone where the instance will be created.
                          type: string
                      required:
                        - diskSize
                        - instanceType
                        - sshKey
                        - template
                        - zone
                      type: object
                  required:
                    - spec
                  type: object
              required:
                - template
              type: object
          type: object
      served: true
      storage: true
