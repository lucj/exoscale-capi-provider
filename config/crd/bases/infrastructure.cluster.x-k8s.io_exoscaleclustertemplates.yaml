---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
    cluster.x-k8s.io/v1beta2: v1beta1
  name: exoscaleclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ExoscaleClusterTemplate
    listKind: ExoscaleClusterTemplateList
    plural: exoscaleclustertemplates
    singular: exoscaleclustertemplate
  scope: Namespaced
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          description: ExoscaleClusterTemplate is the Schema for the exoscaleclustertemplates API.
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              description: ExoscaleClusterTemplateSpec defines the desired state of ExoscaleClusterTemplate.
              properties:
                template:
                  description: ExoscaleClusterTemplateResource defines the template resource for ExoscaleCluster.
                  properties:
                    spec:
                      description: ExoscaleClusterTemplateResourceSpec defines the user-settable cluster fields.
                      properties:
                        masterSecurityGroup:
                          description: MasterSecurityGroup is the name of the security group for control-plane nodes.
                          type: string
                        nodeSecurityGroup:
                          description: NodeSecurityGroup is the name of the security group for worker nodes.
                          type: string
                        zone:
                          description: Zone is the Exoscale zone where cluster resources will be created.
                          type: string
                      required:
                        - zone
                      type: object
                  required:
                    - spec
                  type: object
              required:
                - template
              type: object
          type: object
      served: true
      storage: true
