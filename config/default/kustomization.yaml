namespace: cluster-api-provider-exoscale-system

namePrefix: cluster-api-provider-exoscale-

resources:
  # - ../crd/bases
  - ../rbac
  - ../manager

images:
  - name: controller:latest
    newName: ghcr.io/lucj/exoscale-capi-provider  # overridden by make deploy IMG=...
    newTag: latest
